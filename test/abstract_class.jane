abstract class Parent
{
    name : string;
    abstract duplicate() : string;
}

class Example extends Parent
{
    value1 : int;
    value2 : int;
    duplicate(): string { return this.name + this.name; }
    compute() : int { return this.value1 * this.value2; }
}